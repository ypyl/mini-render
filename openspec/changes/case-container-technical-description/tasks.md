## 1. CaseContainer component

- [x] 1.1 Add `Spoiler` import from `@mantine/core` in `demo/src/components/CaseContainer.tsx`
- [x] 1.2 Render Spoiler between description and children when `technicalDescription` prop is present and non-empty — `maxHeight={0}`, `showLabel="How it works"`, `hideLabel="Hide details"`, content wrapped in `<Text>` with `white-space: pre-wrap`

## 2. Technical descriptions — JSON spec cases

- [x] 2.1 Add `technicalDescription` to `demo/src/cases/basic/spec.json` (CaseContainer props)
- [x] 2.2 Add `technicalDescription` to `demo/src/cases/form/spec.json` (CaseContainer props)
- [x] 2.3 Add `technicalDescription` to `demo/src/cases/actions/spec.json` (CaseContainer props)
- [x] 2.4 Add `technicalDescription` to `demo/src/cases/switch/spec.json` (CaseContainer props)

## 3. Technical descriptions — TypeScript spec cases

- [x] 3.1 Add `technicalDescription` to `demo/src/cases/large/buildSpec.ts` (CaseContainer props)
- [x] 3.2 Add `technicalDescription` to `demo/src/cases/table/buildSpec.ts` (CaseContainer props)
- [x] 3.3 Add `technicalDescription` to `demo/src/cases/detail-modal/buildSpec.ts` (CaseContainer props)
- [x] 3.4 Add `technicalDescription` to `demo/src/cases/two-store/buildSettingsSpec.ts` and `buildPreviewSpec.ts` (CaseContainer props in both)
- [x] 3.5 Add `technicalDescription` to `demo/src/cases/feature-flags/buildSpec.ts` (CaseContainer props)
- [x] 3.6 Add `technicalDescription` to `demo/src/cases/translations/buildSpec.ts` (CaseContainer props)

## 4. Documentation

- [x] 4.1 Update `README.md` demo count and case descriptions if the technical descriptions overlap with existing one-liners
