{
  "name": "mini-render",
  "version": "0.1.0",
  "description": "Minimal spec-driven React UI renderer with granular per-path subscriptions.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10"
  }
}
