// dnd-table/registry.ts — components used by the Drag & Drop table demo.
import { CaseContainer } from "../../components/CaseContainer";
import { DndTable } from "../../components/DndTable";
import type { Registry } from "micro-render";

export const registry: Registry = {
  CaseContainer,
  DndTable,
};
